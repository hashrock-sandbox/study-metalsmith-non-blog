{
  "name": "study-metalsmith-non-blog",
  "private": true,
  "dependencies": {
    "handlebars": "^4.0.5",
    "metalsmith": "^1.0.0",
    "metalsmith-assets": "^0.1.0",
    "metalsmith-layouts": "^1.4.1",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-watch": "^1.0.1"
  },
  "scripts": {
    "build": "node build.js",
    "dev": "parallelshell 'node build.js' 'live-server ./build/'"
  },
  "devDependencies": {
    "live-server": "^0.9.1",
    "parallelshell": "^2.0.0"
  }
}
